
return {
  
  group = 'player',
  weapons = {'flamethrower'},
  
  proto = {
    
  },
  
  constructor = function(entity, ...)
    
  end,
  
  destructor = function(entity, ...)
    
  end,
  
  ai = function(entity)
    
  end,
  
}
